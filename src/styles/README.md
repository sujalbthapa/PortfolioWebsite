# Styles

Centralized styling using Vanilla CSS and modern paradigms.

- `reset.css`: Base resets to ensure cross-browser consistency (formerly `index.css`).
- `global.css`: The primary design system, utility classes, and component-specific styles.
