# Components

Reusable React components organized by their role in the application.

- `/layout`: Structural components that define the shell of the page (Navbar, Footer).
- `/ui`: Generic, presentation-only elements (Buttons, Icons, Bars).
- `/sections`: More complex blocks of content that combine data and UI (Hero, ExperienceList).
